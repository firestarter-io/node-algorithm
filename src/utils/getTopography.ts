/**
 * Firestarter.io
 *
 * Utility functions for getting elevation, slope, and aspect at a given location
 */

import { PointLiteral } from '../types/leaflet.types';

export async function getElevation(point: PointLiteral): number {}
